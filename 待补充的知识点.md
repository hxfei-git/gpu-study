# 待补充的知识点

## 缩写表

| 缩写 | 英文全称 | 中文含义 |
| --- | --- | --- |
| AMDGPU | AMD GPU Linux Kernel Driver | AMD GPU Linux 内核驱动 |
| API | Application Programming Interface | 应用程序编程接口 |
| ARM64 | Arm 64-bit Architecture | 64 位 Arm 架构 |
| AQL | Architected Queuing Language | 架构化队列包格式 |
| BAR | Base Address Register | PCIe 基址寄存器；用于暴露设备地址窗口 |
| BO | Buffer Object | 缓冲对象 |
| COW | Copy-on-Write | 写时复制 |
| CP | Command Processor | GPU 命令处理器 |
| CPU | Central Processing Unit | 中央处理器 |
| CWSR | Compute Wave Save/Restore | 计算 Wave 现场保存与恢复 |
| DMA | Direct Memory Access | 直接内存访问 |
| DRM | Direct Rendering Manager | Linux 直接渲染管理框架 |
| EOP | End of Pipe | 执行结束相关状态机制 |
| ESR_EL1 | Exception Syndrome Register at Exception Level 1 | 异常级别 1 异常综合信息寄存器 |
| FAR_EL1 | Fault Address Register at Exception Level 1 | 异常级别 1 故障地址寄存器 |
| GEM | Graphics Execution Manager | 图形执行管理器 |
| GFP | Get Free Pages | 物理页分配标志 |
| GPU | Graphics Processing Unit | 图形处理器 |
| GPUVA | GPU Virtual Address | GPU 虚拟地址 |
| GPUVM | GPU Virtual Memory | GPU 虚拟地址空间及其页表 |
| GTT | Graphics Translation Table | AMDGPU 中主要表示 GPU 可访问的系统内存域 |
| HMM | Heterogeneous Memory Management | 异构内存管理 |
| HQD | Hardware Queue Descriptor | 硬件队列描述状态 |
| HSA | Heterogeneous System Architecture | 异构系统架构 |
| HSAKMT | HSA Kernel Mode Thunk | ROCr 使用的 HSA 用户态内核接口层 |
| I/O | Input/Output | 输入/输出 |
| IB | Indirect Buffer | 间接命令缓冲区 |
| ioctl | Input/Output Control | 用户态向内核驱动发送控制请求的接口 |
| KFD | Kernel Fusion Driver | Linux AMD GPU 计算驱动接口 |
| LRU | Least Recently Used | 最近最少使用；常用于资源回收顺序 |
| MEC | Micro Engine Compute | AMD GPU 中处理计算队列的命令处理引擎 |
| MMU | Memory Management Unit | 内存管理单元 |
| MMIO | Memory-Mapped Input/Output | 内存映射输入/输出 |
| MQD | Memory Queue Descriptor | 保存在内存中的队列配置描述 |
| PASID | Process Address Space ID | 进程地址空间标识 |
| PDE | Page Directory Entry | 页目录项 |
| PFN | Page Frame Number | 物理页框编号 |
| PTE | Page Table Entry | 页表项 |
| RSS | Resident Set Size | 驻留集大小 |
| ROCr | ROCm Runtime | ROCm 用户态运行时 |
| rptr | Read Pointer | Queue 读指针 |
| SDMA | System Direct Memory Access | AMD GPU 中负责数据搬运和内存写入的专用引擎 |
| SIGSEGV | Segmentation Violation Signal | 段错误信号 |
| SVM | Shared Virtual Memory | 共享虚拟内存 |
| TLB | Translation Lookaside Buffer | 地址翻译缓存 |
| TTM | Translation Table Maps | 转换表映射内存管理器 |
| VMA | Virtual Memory Area | 虚拟内存区域 |
| VMID | Virtual Memory ID | GPU 活动地址空间使用的硬件上下文编号 |
| VRAM | Video Random-Access Memory | GPU 本地显存 |
| wptr | Write Pointer | Queue 写指针 |

> 本文只保留尚未补充的知识点。进入目标阶段前先读取本文；内容写入目标文档并完成校验后，删除对应条目及不再使用的缩写。

## 待补充条目

### 1. Page Fault 各分支与 PTE 更新的完整路径

- 目标阶段或文档：后续 HMM、SVM 与 GPU Page Fault 阶段；届时按需回看 CPU Page Fault，建立两类故障处理的对照。
- 届时需要补充的核心判断：Page Fault 不等于“没有物理页”，而是 CPU 根据当前页表状态无法直接完成本次访问；需要区分物理页缺失、PTE 缺失以及 PTE 权限或状态位不满足访问要求。
- 届时需要补充的公共主线：ARM64 如何通过 `FAR_EL1`、`ESR_EL1` 报告故障；`handle_mm_fault()` 的主要分支；PTE 安装、同步与指令重试。
- 届时需要补充的页面来源与处理分支：
  - 匿名页首次使用时的分配与清零。
  - 文件映射发生缺页时，如何根据文件偏移查找 Page Cache；命中时复用已有物理页并建立 PTE，未命中时由内核发起 I/O、读入页面后再建立 PTE，以及主缺页和次缺页的区别。
  - 匿名页被换出后，非 Present PTE、Swap Entry、Swap Cache、页面换入和 RSS 变化之间的关系。
  - COW 写权限异常中共享只读 PTE、页面复制、PFN 分离，以及页面已独占时可能省略复制的情况。
  - 物理页已经存在时，Access Flag 或其他可恢复权限异常为什么可能只修改 PTE 标志而不分配新数据页；VMA 本身不允许访问时为什么应发送 `SIGSEGV`。
- 当前关联位置：[01_Linux 内存管理基础.md](<./01_Linux 内存管理基础.md>) 的 `2.3.7 VMA 如何参与 Page Fault` 提供 CPU 侧故障基础；[02_GPU 内存管理基础.md](<./02_GPU 内存管理基础.md>) 第 1 章已经给出 GPU 地址翻译成功时的正常路径，后续再与 GPU Page Fault 对照。

### 2. 驱动源码中按需出现的内存接口扩展

- 目标阶段或文档：后续阅读具体 GPU 驱动调用点时按需补充，不再单独展开 Linux 分配器内部算法。
- 尚未补充的范围只保留：
  - 驱动实际使用 `dma_alloc_attrs()`、DMA pool 等扩展 DMA API 时，对应属性和释放规则。
  - 驱动实际使用 `devm_kmalloc()`、`dmam_alloc_coherent()` 等设备托管接口时，它们与设备生命周期的关系。
  - 调用点位于原子或中断上下文时，相关 GFP 标志和接口限制。
  - `folio_alloc()`、`__get_free_pages()`、`get_zeroed_page()` 等变体只有在目标驱动源码实际出现时再比较，不整理成接口大全。
- 当前关联位置：[01_Linux 内存管理基础.md](<./01_Linux 内存管理基础.md>) 的 `4. Linux 内存申请接口的职责边界` 已经覆盖核心接口选择、连续性、DMA 地址以及“申请”和“映射”的区别。

### 3. `malloc()` 如何处理多线程分配

- 目标阶段或文档：用户态内存申请与 `malloc()` 分配器阶段。
- 届时需要补充：分配器 arena（分配区）、线程缓存和元数据锁；如何避免重复分配同一内存块；跨线程传递和释放内存；调试工具如何记录“由哪个线程申请”，以及这种记录为什么不是 MMU 访问隔离。
- 当前关联位置：[01_Linux 内存管理基础.md](<./01_Linux 内存管理基础.md>) 的 `2.1.5 同一地址空间中的内存属于哪个线程`。

### 4. 通用 DRM/GEM/TTM 机制与普通 IB 提交路径

- 目标阶段或文档：以后明确学习 Linux DRM、通用 GPU 内存管理或图形命令提交时，单独建立可选专题。
- 当前已经保留的边界：[02_GPU 内存管理基础.md](<./02_GPU 内存管理基础.md>) 第 2 章只解释 KFD 内存路径所需的 `kgd_mem → amdgpu_bo`、`amdgpu_bo_va → amdgpu_vm`、GPUVM 映射以及最小 Ring BO 引用案例；第 3 章只解释内存可见性。下面登记超出当前内存基础范围的通用实现。
- 延后原因：完整 DRM handle、GEM/TTM 对象体系和 DRM scheduler 的 IB 提交会引入大量图形框架与通用内存管理分支；它们有助于以后系统掌握 AMDGPU，但不是理解当前 AQL Dispatch 正常路径的前置条件。
- 届时需要补充的对象与生命周期：
  - 每次打开 DRM 设备文件所拥有的 handle 表，以及 handle 与 `drm_gem_object` 的查找关系。
  - `drm_gem_object`、`ttm_buffer_object`、`amdgpu_bo` 的包含关系和各自职责。
  - 通用 DRM handle 创建、删除、文件关闭、跨进程共享以及最后一个引用触发最终 BO 销毁的完整关系；不再重复 AQL Queue 已经讲过的局部引用持有。
- 届时需要补充的存储与映射：
  - TTM placement、资源管理器、LRU、驱逐和 validation 怎样在 GTT 与 VRAM 之间选择或迁移放置位置；系统页面、分散页面描述、DMA 地址与本地显存资源怎样成为存储后端。
  - `remap_pfn_range()`、`vmf_insert_page()`、`vmf_insert_pfn()`、驱动 VMA fault 回调以及 CPU 可见/不可见显存的访问边界。
  - 通用 DRM BO 怎样登记到 GPUVA、怎样选择系统页面 DMA 地址或本地显存地址生成 GPU PTE，以及迁移前后怎样更新 PTE、TLB 和同步状态。
  - GPUVM 页表 BO 自身怎样在 VRAM/GTT 中放置；`vm_update_mode` 怎样为计算 GPUVM 选择 CPU 或 SDMA 更新后端；CPU 更新对 BAR 可见性的要求，以及 SDMA 私有页表更新 IB/job、调度、Fence 和错误路径。必须明确这些是 Host Driver 的 GPUVM 实现细节，不是每个 AQL Packet 的执行路径。
  - 学习 Linux `dma_fence` 与 AMDGPU 异步完成通知时，沿页表更新案例补全：`dma_fence` 的 context、sequence number、signaled 状态、回调和等待者分别表示什么；它为什么是“异步任务完成凭证”，而不是 CPU/GPU 内存顺序屏障、AQL fence scope 或 HSA Completion Signal。
  - 补全页表更新 Fence 的软件对象链：SDMA 页表更新 job 返回的 Fence 怎样成为 `bo_va->last_pt_update`，怎样由 `amdgpu_sync_fence()` 收集进 `kgd_mem->sync`，以及 KFD 外层怎样通过 `amdgpu_amdkfd_gpuvm_sync_memory()`、`amdgpu_sync_wait()` 和 `dma_fence_wait()` 等待完成。对照说明 CPU 后端同步写 PTE 时为什么通常不需要等待新的异步硬件 Fence。
  - 补全 SDMA 完成后的通知链：队尾硬件 Fence 命令怎样写入完成序号并请求中断；AMDGPU 怎样读取已完成序号、调用 `dma_fence_signal()`，再经 DRM scheduler 的 finished Fence 传播并唤醒等待 MAP ioctl 的内核线程；同时说明中断丢失时的 fallback 检查，以及等待参数允许被 Linux 用户信号中断与“硬件完成通知”不是一回事。
  - 最终重新连接 GPUVM MAP 的顺序要求：先确认页表 BO 的 PDE/PTE 写入 Fence 已完成，再 invalidate 旧 TLB 翻译，最后才向 HSAKMT 返回 MAP 成功；解释为什么 Fence 完成、TLB invalidate 和数据 Cache write-back 是三个不同动作。
  - `kgd_mem->attachments` 的通用多关系模型：旧 AQL 双重映射为什么可能把同一 BO 映射到两段 VA；跨 GPU 映射为什么可能建立 SG 或 DMA-BUF 辅助 BO；多个映射管理对象为什么不自动等于复制了多份 backing 数据。
- 届时需要补充的普通 DRM 提交链：从用户态命令提交到 `drm_sched_job`、`amdgpu_job_run()`、`amdgpu_ib_schedule()`、GPUVM flush、`amdgpu_ring_emit_ib()` 与 `amdgpu_ring_commit()`；明确 IB 是普通 GPU ring 上的命令缓冲，而不是 AQL Ring 中的 AQL Packet。
- 届时必须做的对照：

  ```text
  AQL compute：ROCr/KFD → AQL Ring → Doorbell → CP/MEC
  普通 DRM 命令提交：DRM scheduler/job → IB → GPU ring
  ```

  两条路径最后都可能使用 GPUVM、PTE 和 TLB，但“怎样提交工作”不同，不能把普通 DRM job 的执行链当成 AQL Packet 的执行链。
- 源码核对重点：本地 Linux 的 `include/drm/drm_file.h`、`drivers/gpu/drm/drm_gem.c`、`include/drm/ttm/`、`drivers/gpu/drm/amd/amdgpu/amdgpu_gem.c`、`amdgpu_object.h`、`amdgpu_vm.c`、`amdgpu_job.c` 与 `amdgpu_ib.c`。
- 当前关联位置：[01_Linux 内存管理基础.md](<./01_Linux 内存管理基础.md>) 的 `2.3.8 驱动映射为什么也需要 VMA` 提供 CPU 侧映射基础；[02_GPU 内存管理基础.md](<./02_GPU 内存管理基础.md>) 第 1 章覆盖地址翻译，第 2 章覆盖 KFD 所需的最小 BO/GPUVM 关系。通用 DRM/TTM 内存管理和 DRM job/IB VM flush 链仍延后到本专题。

### 5. AQL Queue 创建、驻留与完整 Dispatch 路径

- 目标阶段或文档：完成 GPU 内存基础后，单独建立 AQL Dispatch 主线文档；以 ROCr→KFD→硬件 Queue→Kernel→Completion Signal 为主线。
- 延后原因：[02_GPU 内存管理基础.md](<./02_GPU 内存管理基础.md>) 只需要用 AQL Ring 解释映射生命周期，用 Packet→Doorbell 解释内存顺序。继续展开 Queue 配置和执行会让内存基础失去主线。
- 届时需要补充的 Queue 创建路径：
  - ROCr 怎样分配 Ring、rptr、wptr、EOP 与 CWSR 等资源，并把地址传入 libhsakmt/KFD。
  - KFD `CREATE_QUEUE` 怎样验证 Ring/rptr/wptr 的 GPUVM 映射、取得 BO 引用并准备 Doorbell。
  - MQD 怎样保存 Ring base、指针、Doorbell、PASID/VMID 等配置；MQD 装载为 HQD 后，Queue 怎样成为活动硬件状态。
  - 固件调度与非固件调度下的 Queue 驻留、PASID→VMID 选择及代际差异；不要把某一种 GFX9 路径推广为所有硬件的固定实现。
- 届时需要补充的 Dispatch 数据路径：
  - CPU 怎样预留 Ring 槽位并填写完整 AQL Kernel Dispatch Packet，包括 `kernel_object`、Kernarg 和 Completion Signal。
  - 有效 header 的 release 发布、Doorbell MMIO、CP/MEC 取包与 GPU 地址翻译怎样连接。
  - Kernel 怎样读取参数和用户数据、写结果，再通过 release fence 与 Completion Signal 让 CPU acquire 观察完成。
  - 一次 Packet 完成与整条 Queue 销毁的生命周期区别，以及异常、hang、reset 和销毁错误路径。
- 届时必须保持的边界：AQL Ring 不是普通 DRM IB；AQL Queue 路径可以复用 GPUVM 和 BO，但不能套用 `drm_sched_job → amdgpu_ib_schedule()` 作为 Packet 执行链。
- 当前关联位置：[02_GPU 内存管理基础.md](<./02_GPU 内存管理基础.md>) 的 `2.7 AQL Ring案例` 已提供映射与引用前提，`3.6 AQL发布案例` 和 `3.7 GPU完成后的结果可见性` 已提供内存顺序前提；后续文档应从这些结论继续，不重复扩写内存基础。
