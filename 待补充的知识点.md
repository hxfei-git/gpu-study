# 待补充的知识点

## 缩写表

| 缩写 | 英文全称 | 中文含义 |
| --- | --- | --- |
| ARM64 | Arm 64-bit Architecture | 64 位 Arm 架构 |
| COW | Copy-on-Write | 写时复制 |
| CPU | Central Processing Unit | 中央处理器 |
| DMA | Direct Memory Access | 直接内存访问 |
| ESR_EL1 | Exception Syndrome Register at Exception Level 1 | 异常级别 1 异常综合信息寄存器 |
| FAR_EL1 | Fault Address Register at Exception Level 1 | 异常级别 1 故障地址寄存器 |
| GEM | Graphics Execution Manager | 图形执行管理器 |
| GPU | Graphics Processing Unit | 图形处理器 |
| MMU | Memory Management Unit | 内存管理单元 |
| PA | Physical Address | 物理地址 |
| PFN | Page Frame Number | 物理页框编号 |
| PTE | Page Table Entry | 页表项 |
| TLB | Translation Lookaside Buffer | 地址转换后备缓冲器（快表） |
| TTM | Translation Table Maps | 转换表映射内存管理器 |
| VA | Virtual Address | 虚拟地址 |
| VMA | Virtual Memory Area | 虚拟内存区域 |

> 本文只保留尚未补充的知识点。进入目标阶段前先读取本文；内容写入目标文档并完成校验后，删除对应条目及不再使用的缩写。

## 待补充条目

### 1. Page Fault、物理页分配与 PTE 更新的完整路径

- 目标阶段或文档：下一阶段“物理页、PFN、`struct page` 与 Page Fault”。
- 届时需要补充：ARM64 如何通过 `FAR_EL1`、`ESR_EL1` 报告故障；`handle_mm_fault()` 的主要分支；匿名页分配与清零；物理页、`struct page` 和 PFN 的关系；PTE 安装、同步与指令重试；COW、文件页、Page Cache 和交换页的差异。
- 当前关联位置：[Linux 内存管理基础.md](<./Linux 内存管理基础.md>) 的 `2.3.7 VMA 如何参与 Page Fault`。

### 2. Linux 内存申请接口与“连续”的不同含义

- 目标阶段或文档：物理页分配器与 Linux 内存申请接口章节。
- 届时需要补充：`alloc_pages()`、`kmalloc()`、`vmalloc()`、`dma_alloc_coherent()` 的使用对象和返回地址；CPU VA、PA、设备 DMA 地址分别是否连续；连续页申请与物理内存碎片的关系。
- 当前关联位置：[Linux 内存管理基础.md](<./Linux 内存管理基础.md>) 的 `2.10 地址连续性的边界说明`。

### 3. `malloc()` 如何处理多线程分配

- 目标阶段或文档：用户态内存申请与 `malloc()` 分配器阶段。
- 届时需要补充：分配器 arena（分配区）、线程缓存和元数据锁；如何避免重复分配同一内存块；跨线程传递和释放内存；调试工具如何记录“由哪个线程申请”，以及这种记录为什么不是 MMU 访问隔离。
- 当前关联位置：[Linux 内存管理基础.md](<./Linux 内存管理基础.md>) 的 `2.1.5 同一地址空间中的内存属于哪个线程`。

### 4. 驱动 VMA、PFN 插入与 GEM/TTM 映射

- 目标阶段或文档：GPU 驱动用户映射以及 GEM、TTM 内存对象章节。
- 届时需要补充：`remap_pfn_range()`、`vmf_insert_page()`、`vmf_insert_pfn()` 的使用边界；驱动 VMA 的 fault 回调；一次性 PFN 映射与按需插页的差异；这些机制如何连接 GEM、TTM 对象。
- 当前关联位置：[Linux 内存管理基础.md](<./Linux 内存管理基础.md>) 的 `2.3.8 驱动映射为什么也需要 VMA`。
