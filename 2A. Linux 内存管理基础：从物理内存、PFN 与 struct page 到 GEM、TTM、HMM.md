# Linux 内存管理基础：从 CPU 虚拟地址到物理页

> **[BOUNDARY]** 此文件是根目录中的兼容副本；规范版本位于 [1.笔记/2A](<./1.笔记/2A. Linux 内存管理基础：从物理内存、PFN 与 struct page 到 GEM、TTM、HMM.md>)。

> 本文是整套串联文档的**内存对象地基**。先完整讲清一个普通 CPU 进程如何使用一页
> RAM，然后再以这一页为起点，逐层解释设备访问内存时需要增加什么。

源码路径口径：本文未显式写仓库根的 Linux 路径均相对于 `2.源码/linux`；libhsakmt 路径相对于 `2.源码/rocr-runtime`。源码基线分别为 `248951ddc14de84de3910f9b13f51491a8cd91df` 与 `ba56a24c6132c5d195686ae4adf969ca1222fbba`。

读完前三节，应当先能独立回答：

1. `malloc()` 返回的是什么，它是不是物理地址？
2. CPU VA、VMA、PTE、PFN 和 PA 如何连接？
3. `struct page *` 指向页面数据，还是指向 Linux 的页面元数据？
4. 预留了一段 CPU VA，是否意味着所有物理页都已经存在？

---

## 0. 先只看 CPU：一次普通内存访问经过什么

暂时不考虑任何设备。用户程序执行 `malloc()` 或匿名 `mmap()` 后，最基础的路径是：

```text
用户程序 malloc()/mmap()
          │
          ▼
        CPU VA
          │ VMA描述该区间的权限和来源
          ▼
      CPU首次访问
          │ 若PTE尚未present（not-present），触发page fault
          ▼
 分配/取得page frame
          │
          ├── PFN/PA：物理页编号与物理地址
          └── struct page：Linux对该物理页的元数据
          │
          ▼ 写入CPU PTE
       CPU访问RAM
```

这张图已经是一条完整的普通 CPU 内存路径。接下来的第 1～3 节只是展开图中的对象，
不需要借助任何设备专用内存管理框架。

先记住两个边界：

```text
CPU VA 不是 PA
PFN 不是 struct page *
```

下面从 page frame 的物理地址与编号开始。

---

## 1. 物理内存：PA、page frame、PFN 和页内偏移

上一节留下的第一个问题是：CPU 页表最终到底指向什么？答案不是“一个抽象内存对象”，而是
Host physical address space 中的 page frame。

### 1.1 PA 是字节地址，PFN 是页编号

PA（Physical Address）以 byte 为单位；PFN（Page Frame Number）以 page 为单位。

Linux 直接定义：

```c
#define PFN_PHYS(x) ((phys_addr_t)(x) << PAGE_SHIFT)
#define PHYS_PFN(x) ((unsigned long)((x) >> PAGE_SHIFT))
```

源码：`include/linux/pfn.h:12-13`。

因此：

```text
PFN          = PA >> PAGE_SHIFT
page base PA = PFN << PAGE_SHIFT
完整 PA      = page base PA + page offset
```

### 1.2 用一个 4 KiB 页面算到底

假设：

```text
PAGE_SHIFT = 12
PAGE_SIZE  = 0x1000 = 4 KiB
PA         = 0x12345ABC
```

那么：

```text
PFN         = 0x12345ABC >> 12 = 0x12345
page offset = 0x12345ABC & 0xFFF = 0xABC
page base PA= 0x12345 << 12 = 0x12345000

PA = 0x12345000 + 0xABC
```

所以不能把 PFN 与 PA 当作同一种地址值。准确说法是：

> PFN 是去掉页内偏移、以 page 为单位编码的物理页编号；PA 是以 byte 为单位的完整物理地址。

### 1.3 PFN 不保证对应普通 RAM

CPU physical address space 不只放 DRAM，还可能包含：

```text
RAM
固件保留区
PCIe BAR / MMIO
地址空洞
持久内存或 ZONE_DEVICE 页面
```

因此不能对任意物理地址都假设存在普通 `struct page`。对 Linux 正常管理的 RAM，PFN 与
`struct page` 可以转换；对 MMIO hole 或某些设备地址则不成立。这也是后面不能拿 BAR PA
直接当普通 RAM page 使用的原因。

本节已经得到 PFN，但 Linux 不能只靠一个数字管理引用计数、回收、LRU 和映射状态。于是必须
引入下一层：`struct page`。

---

## 2. `struct page`：物理页的内核档案，不是页面数据

### 2.1 `struct page` 描述 page frame

`struct page` 是 Linux 为可管理物理页建立的元数据对象，定义在：

```text
include/linux/mm_types.h:80
```

它包含或复用的典型信息包括：

```text
flags       页面状态、zone 等
_refcount   引用计数
mapping     文件页/匿名页等映射关系
lru         回收与LRU链表关系
private     特定子系统私有状态
```

应当把它理解成：

```text
Host RAM中的4 KiB数据页              内核元数据区
PA 0x12345000                     struct page
┌─────────────────────┐           ┌────────────────────┐
│ 真正的数据 bytes      │  ◄──────► │ flags/refcount/... │
└─────────────────────┘   描述     └────────────────────┘
```

`struct page *` 是**指向元数据的 Kernel VA**，既不是页面数据地址，也不是这个 page frame 的 PA。

### 2.2 PFN 与 `struct page` 如何互换

对正常 RAM：

```c
struct page *page = pfn_to_page(pfn);
unsigned long pfn = page_to_pfn(page);
phys_addr_t pa = page_to_phys(page);
```

源码关系：

```c
#define page_to_pfn  __page_to_pfn
#define pfn_to_page  __pfn_to_page
#define page_to_phys(page) PFN_PHYS(page_to_pfn(page))
```

位置：`include/asm-generic/memory_model.h:73-85`。

不同 Linux memory model 的实现不同：

```text
FLATMEM：   mem_map + PFN offset
VMEMMAP：   vmemmap + PFN
SPARSEMEM：先定位 section，再定位 section 内 page
```

它们实现不同，但抽象保持一致：

```text
PFN  ←──────→  struct page
编号           Linux管理对象
```

以现代 64 位 Linux 常用的 `SPARSEMEM_VMEMMAP` 为例，`vmemmap` 可以抽象成一个以 PFN 为下标的
`struct page` 数组：

```c
#define __pfn_to_page(pfn)  (vmemmap + (pfn))
#define __page_to_pfn(page) ((unsigned long)((page) - vmemmap))
```

因此同一个 PFN 把数据页与它的元数据联系起来：

```text
PFN = N
  ├─ 数据页起始PA = N << PAGE_SHIFT
  └─ 页面元数据   = &vmemmap[N]
```

`struct page` 通常不再单独保存一份完整 PFN；`page_to_pfn()` 根据它在 `vmemmap` 中的位置反推数组
下标。这里的 `&vmemmap[N]` 是元数据对象的 Kernel VA，`N << PAGE_SHIFT` 才是被它描述的
数据页 PA。

Linux 确实通常为每个可管理的 base page 准备一个 `struct page`，但不是“每个数据页再占用
一个完整元数据页”。假设 4 KiB/page、`sizeof(struct page) == 64`：

```text
一个4 KiB元数据页可容纳 4096 / 64 = 64个struct page
元数据开销约为 64 / 4096 = 1.5625%
1 GiB RAM对应约16 MiB struct page元数据
```

`sizeof(struct page)` 随架构和配置变化；`SPARSEMEM_VMEMMAP` 还会按实际存在的 memory section 建立
backing，避免按整个巨大物理地址空间为地址空洞全部分配元数据。

### 2.3 buddy allocator 分配的是 page frame

Linux buddy allocator 以 2 的 order 管理连续 page frame。类似：

```c
struct page *page = alloc_pages(gfp_mask, order);
```

返回的是第一页的 `struct page *`。若 `order=2`，表示连续分配：

```text
2^2 = 4 pages = 16 KiB（假设4 KiB/page）
```

从抽象关系上看，若分配到的首页 PFN 是 `N`，那么返回值确实等价于：

```c
struct page *page = &vmemmap[N];
```

但 buddy allocator 的实际工作方向通常不是“先找到 PFN，再查 `vmemmap`”。zone 的 buddy free list
本身就用 `struct page` 组织空闲块：

```text
alloc_pages(gfp, order)
  → get_page_from_freelist()
  → rmqueue()
  → __rmqueue_smallest()
  → 从zone->free_area[order]取出struct page
  → 必要时拆分更大的buddy block
  → 返回首页的struct page *
```

`__rmqueue_smallest()` 直接从 free area 取得 `struct page *page` 并返回。只有当后续代码需要页号或地址时，
才通过：

```c
unsigned long pfn = page_to_pfn(page);
phys_addr_t pa = page_to_phys(page);
```

源码入口：`mm/page_alloc.c:1889 __rmqueue_smallest()`、
`mm/page_alloc.c:3792 get_page_from_freelist()`。

所以：

```text
buddy allocator 决定“哪些物理页归这次分配”
struct page 记录“这些物理页当前是什么状态”
PFN 给出“这些物理页在Host physical空间中的编号”
```

现在我们已经知道物理页及其元数据。下一步要回答：用户进程拿到的 CPU VA 如何关联到这些页？

---

## 3. CPU 虚拟内存：`mm_struct`、VMA、PTE 与 page fault

### 3.1 `mm_struct` 管地址空间，VMA 管区间

一个进程的用户地址空间由 `mm_struct` 代表。VMA（`vm_area_struct`）描述一段具有共同属性的
虚拟地址区间：

```text
mm_struct
  ├─ VMA: code      r-x
  ├─ VMA: heap      rw-
  ├─ VMA: mmap区域  rw-/file-backed/anonymous
  └─ VMA: stack     rw-
```

VMA 回答的是：

```text
这段VA是否合法？
允许读写还是执行？
匿名映射还是文件映射？
发生fault时应从哪里取得页面？
```

VMA 本身通常不记录“每个 VA 当前对应哪个 PFN”。逐页翻译关系在 CPU page table/PTE 中。

进程、线程与 `mm_struct` 的所有权关系见
[Linux 进程基础](<./1.笔记/3. Linux 进程基础：task_struct、mm 与线程.md>)。

### 3.2 PTE 把虚拟页映射到物理页

忽略多级页表中间层后，一条 CPU 映射可以压缩为：

```text
CPU VA = virtual page number + page offset
                       │
                       ▼
CPU PTE = PFN + permission/status flags
                       │
                       ▼
Host PA = (PFN << PAGE_SHIFT) + page offset
```

CPU PTE 里除了 PFN，还包含 present、writable、dirty、accessed 等状态。页表结构、页表页和 TLB
在 [MMU 与页表专题](<./1.笔记/7. MMU 与页表：从 CPU 对照看 GPUVM 的构建、写入与切换.md>) 中展开。

### 3.3 `mmap()` 成功不等于已经分配所有物理页

匿名 `mmap()` 或堆扩展通常先建立 VMA、预留 VA 范围。实际 page frame 可以在第一次访问时，
由 page fault 路径按需分配：

```text
mmap / brk
  → 建立VMA
  → 返回CPU VA
  → 此时部分PTE可能仍not-present

CPU第一次load/store
  → page fault
  → handle_mm_fault()
  → 分配/取得struct page
  → 写CPU PTE
  → 指令重试
```

内核入口：`mm/memory.c:6651 handle_mm_fault()`。

这解释了为什么“用户预留 VA”不能回答“物理 PA 在哪里”：VA reservation 与 backing page
population 是两个阶段。

### 3.4 常见分配接口到底返回什么

| 接口                | 调用者 | 直接返回          | 物理连续性         | 物理页通常何时出现                            |
| ------------------- | ------ | ----------------- | ------------------ | --------------------------------------------- |
| `malloc()`        | 用户态 | CPU user VA       | 不保证             | allocator扩堆/`mmap`后，常在fault时按需出现 |
| anonymous`mmap()` | 用户态 | CPU user VA范围   | 不保证             | 常在首次访问fault时出现                       |
| `alloc_pages()`   | 内核   | `struct page *` | 按order连续        | 调用成功时已经分配                            |
| `kmalloc()`       | 内核   | Kernel VA         | 小对象通常物理连续 | 调用成功时已有slab/page backing               |
| `vmalloc()`       | 内核   | 连续Kernel VA     | 物理页可离散       | 调用过程中分配并建立内核页表映射              |

特别注意：

```text
malloc返回的不是PA/PFN
alloc_pages返回的不是页面数据User VA
vmalloc连续的是Kernel VA，不是Host PA
```

现在 CPU 已经能通过自己的页表访问 page frame。但网卡、NVMe 或 GPU 这类 DMA 设备
既不能解引用 CPU VA，也不认识内核中的 `struct page *`。因此下一节先引入所有这类设备共用的
DMA 地址层，它还不是 GPU 专用机制。

---

## 4. 加入普通 DMA 设备：从 `struct page` 到 SG、DMA address 与 IOVA

CPU-only 模型缺少的不是物理页，而是**针对某个设备的可用地址和访问合同**：

```text
CPU已有： CPU VA → CPU PTE → PFN/PA ↔ struct page

仍然缺少：
  1. 如何描述一批物理不连续的page？
  2. 这个具体device应该使用什么地址？
  3. IOMMU映射、cache同步和访问生命周期由谁管理？
```

Linux 的通用 DMA API 补上这一层。

### 4.1 CPU physical address 不等于设备可用地址

设备驱动必须通过 Linux DMA API 建立设备视角：

```text
struct page[]
  → scatter-gather table
  → dma_map_page()/dma_map_sgtable()
  → dma_addr_t
```

`dma_addr_t` 表示**针对某个具体 device，设备应放进 DMA request 或设备页表的地址**。

不能把以下对象互换：

```text
struct page *   内核元数据指针
PFN             Host物理页编号
Host PA         CPU physical address
dma_addr_t      device-visible address
```

### 4.2 SG 为什么存在

一块逻辑上连续的 buffer，物理页可能离散：

```text
buffer offset 0      → PFN 0x1000
buffer offset 4 KiB  → PFN 0x8A20
buffer offset 8 KiB  → PFN 0x8A21
buffer offset 12 KiB → PFN 0x3100
```

SG（scatter-gather）表把这些 page/segment 组织起来，并可把物理连续的相邻页合并成更大的段。
驱动随后对 SG 做 DMA mapping，DMA layer 还可能在保持 buffer 逻辑顺序的前提下，再次合并
连续的设备可见 segment。

因此需要区分：

```text
sg_page(sg)          segment背后的struct page
sg_phys(sg)          CPU physical视角（并非所有驱动都应直接使用）
sg_dma_address(sg)   dma_map_*之后的device-visible地址
```

SG 的核心价值是将一个逻辑 buffer 表示为**按 buffer offset 排序的 `(segment, length)` 清单**，
而不是强行让所有页变得物理连续。假设四个 page 的 PFN 依次是：

```text
0x100, 0x900, 0x901, 0x300
```

`sg_alloc_table_from_pages()` 看到 `0x900` 和 `0x901` 物理相邻，因此可以得到：

```text
SG[0] = { first_page=PFN 0x100, length=4 KiB }
SG[1] = { first_page=PFN 0x900, length=8 KiB }  // 两个相邻page
SG[2] = { first_page=PFN 0x300, length=4 KiB }
```

即使四个 page 全部不相邻，SG 仍然有用：它会保留四个 entry，让驱动按顺序生成四个 DMA
descriptor。合并只是减少 descriptor 数量和硬件切换次数的优化，不是 SG 成立的前提。

#### 4.2.1 通用驱动的 SG/DMA API 完整调用顺序

以支持 hardware SG descriptor 的普通设备为例。假设驱动已经拥有 `pages[]`、`n_pages`、buffer
起始页内偏移 `offset` 和有效字节数 `size`，一次 streaming DMA 的骨架是：

```c
struct sg_table sgt;
struct scatterlist *sg;
unsigned int i;
int ret;

/* 1. pages[] -> 未做DMA mapping的SG */
ret = sg_alloc_table_from_pages(&sgt, pages, n_pages,
                                offset, size, GFP_KERNEL);
if (ret)
    return ret;

/* 2. page/physical视角 -> 这个device可用的DMA视角 */
ret = dma_map_sgtable(dev, &sgt, DMA_TO_DEVICE, 0);
if (ret)
    goto free_sg;

/* 3. 只遍历mapping后的DMA segment，填硬件descriptor */
for_each_sgtable_dma_sg(&sgt, sg, i) {
    dma_addr_t addr = sg_dma_address(sg);
    size_t len = sg_dma_len(sg);

    program_dma_descriptor(addr, len); /* 伪代码：设备专用操作 */
}

start_dma_and_wait_for_completion();   /* 伪代码 */

/* 4. DMA结束后解除设备映射 */
dma_unmap_sgtable(dev, &sgt, DMA_TO_DEVICE, 0);
free_sg:
sg_free_table(&sgt);
```

这些 API 之间传递的关键状态是：

```text
pages[]
  │ sg_alloc_table_from_pages()
  ▼
sgt.sgl + sgt.orig_nents
  │   每个entry是page + offset + length
  │ dma_map_sgtable(dev, ...)
  ▼
sgt.nents
  │   每个有效entry现在有sg_dma_address/sg_dma_len
  │ for_each_sgtable_dma_sg()
  ▼
硬件descriptor[]
  ▼
DMA engine依次处理各segment
```

`orig_nents` 是 DMA mapping 前的 SG entry 数；`nents` 是 mapping 后设备真正需要处理的 DMA
segment 数。IOMMU 或物理相邻性可能让 DMA layer 继续合并 entry，而设备的最大 segment 长度和
boundary 限制则决定哪些 entry 允许合并。`dma_map_sgtable()`
成功时返回 0，并把 mapping 后数量写入 `sgt.nents`。

如果直接使用较底层的 `dma_map_sg()`，它的返回值就是 mapping 后的 segment 数 `count`；
遍历时用 `count`，但 `dma_unmap_sg()` 必须传回 mapping 前的原始 `nents`。`dma_map_sgtable()`/
`dma_unmap_sgtable()` 封装了这个容易传错的区别。

对可重复使用的 streaming mapping，若 CPU 要在两次 DMA 之间访问 buffer，还需要按所有权方向调用：

```text
DMA完成 → dma_sync_sgtable_for_cpu()    → CPU可安全访问
CPU修改 → dma_sync_sgtable_for_device() → device可再次DMA
```

源码/API 入口：

```text
include/linux/scatterlist.h:495 sg_alloc_table_from_pages()
kernel/dma/mapping.c:331       dma_map_sgtable()
include/linux/scatterlist.h:232 for_each_sgtable_dma_sg()
include/linux/dma-mapping.h:562 dma_unmap_sgtable()
```

#### 4.2.2 AMDGPU USERPTR 如何消费同一套 API

AMDGPU 不是把 SG entry 直接当作 CPU 页表使用。USERPTR bind 路径先用通用 DMA API 得到
device-visible address，再将可能长于一页的 SG segment 展开成 GPUVM 方便逐页消费的数组：

```text
HMM查询CPU VA
  → ttm->pages[]
  → sg_alloc_table_from_pages(ttm->sg, ttm->pages, ...)
  → dma_map_sgtable(adev->dev, ttm->sg, direction, 0)
  → SG entry中得到sg_dma_address/sg_dma_len
  → drm_prime_sg_to_dma_addr_array()
  → gtt->ttm.dma_address[0..num_pages-1]
  → AMDGPU VM逐页写GPU PTE
```

所以在 AMDGPU 中，SG 先用于压缩和 DMA-map 一批离散 Host page；后面又展开成逐页
`dma_address[]`。“先合并、后展开”并不矛盾：前者让 DMA API/IOMMU 用更少 segment 建立映射，
后者让 GPU 页表每个 PTE 都能取到自己的输出地址。

对应源码：`drivers/gpu/drm/amd/amdgpu/amdgpu_ttm.c:811-840`。

### 4.3 无 IOMMU 与有 IOMMU

```text
无Host IOMMU / direct DMA：
  dma_addr_t 数值通常接近Host PA

有Host IOMMU：
  struct page / Host PA
       ▲
       │ Host IOMMU页表
  IOVA = dma_addr_t
       ▲
       │ device发出request
     device
```

所以正确链条是：

```text
CPU VA → CPU PTE → PFN → Host PA

Device/DMA side：dma_addr_t(IOVA) → Host IOMMU → 同一个Host PA
```

IOMMU 的地址隔离、PCIe transaction 和 DMA mask 见
[AMD PCIe 地址映射与 DMA](<./1.笔记/10. AMD PCIe 地址映射与 DMA：GPU Endpoint.md>)。

### 4.4 DMA mapping 还承载生命周期和一致性合同

`dma_map_*()` 不只是“算一个地址”，它还建立：

```text
这个device可以访问哪些page
访问方向（TO_DEVICE/FROM_DEVICE/BIDIRECTIONAL）
是否需要cache同步
IOMMU映射何时有效
什么时候必须dma_unmap_*()
```

因此，即使能从 PFN 算出 Host PA，驱动也不能绕过 DMA API 把它直接交给设备。

到这里，普通设备已经能通过下面的链条访问 Host RAM：

```text
struct page[] → SG → dma_map_*() → dma_addr_t/IOVA → device DMA
```

网卡和 NVMe 驱动可以使用这条通用路径，并不需要 TTM。**TTM 不参与普通 CPU 内存管理**，
也不是通用 DMA 的必经层；它是 DRM/GPU 驱动用来管理 Buffer Object backing、放置和迁移的框架。

### 4.5 第二张图：GPU 在 CPU 和通用 DMA 之上增加了什么

下图不再把所有对象画成一条必经直线，而是标出每层相对上一层的新增部分：

```text
CPU 已有
  CPU VA → VMA / CPU PTE → PFN/PA ↔ struct page[] → Host RAM
                                             │
                                             │ 同一批Host page
                                             ▼
通用 DMA 新增
                                    SG → dma_map_*()
                                             │
                                             ▼
                                      dma_addr_t / IOVA
                                             │
                                             ▼
GPU 新增
  对象身份与共享 ──────────────────── GEM
  backing、放置与迁移 ─────────────── TTM
  CPU页表查询与失效跟踪 ─────────── HMM
  per-process GPU虚拟地址 ────────────── GPUVM
                                             │
                                             ▼
                                  GPU PTE → GPUVA → GPU访问
```

通用 DMA 只解决“这个 device 用什么地址访问这批 page”。GPU 还要向用户态暴露内存对象，
在 VRAM 与 system RAM 之间放置或迁移数据，跟踪由 CPU 页表控制的用户页，并维护每个进程的
GPU 虚拟地址空间。下面才逐个引入这四个 GPU 扩展。

---

## 5. GEM：给 GPU 内存对象一个身份，不决定页面住哪里

GEM（Graphics Execution Manager）首先解决的是对象/API 问题：用户态不能拿一个内核指针作为
GPU buffer handle，内核也需要引用计数、mmap 和跨进程共享机制。

### 5.1 GEM 的输入、状态与输出

```text
输入：用户态创建/查找/mmap/共享一个GPU内存对象

GEM内部状态：
  drm_gem_object
  size
  refcount
  per-file handle
  mmap vma_node/token
  dma-buf导出/导入关系

输出：一个有生命周期和访问权限的内核对象引用
```

核心定义与入口：

```text
include/drm/drm_gem.h:277       struct drm_gem_object
drivers/gpu/drm/drm_gem.c:552  drm_gem_handle_create()
drivers/gpu/drm/drm_gem.c:1377 drm_gem_mmap()
```

### 5.2 GEM 不回答 backing 在哪里

GEM 可以告诉你：

```text
这个handle属于哪个drm_file
对象大小是多少
还有多少引用
用户mmap时对应哪个对象
```

但通用 GEM 不决定：

```text
数据在VRAM还是system RAM
有哪些struct page
是否要从VRAM迁移到GTT
某个GPUVA映射到哪里
```

这些由驱动专用内存管理器和 VM 层完成。在 AMDGPU 中，`amdgpu_bo` 内嵌 TTM BO，而
TTM BO 又内嵌 GEM object：

```text
struct amdgpu_bo
  └─ struct ttm_buffer_object tbo
       └─ struct drm_gem_object base
```

于是同一个 buffer 同时拥有 GEM 身份和 TTM backing 管理状态。

---

## 6. TTM：管理 BO backing、放置和迁移

TTM（Translation Table Manager）承接 GEM 留下的问题：这个 BO 的数据究竟住在哪里，system
backing 有哪些 page，设备访问地址是什么，内存压力下能否迁移或换出？

“backing 住在 system RAM”并不意味“只由普通 CPU 虚拟内存路径管理”。物理 page frame 仍然由
Linux MM/buddy allocator 提供，但若这批 page 是一个 GPU BO 的 backing，TTM 就要管理它们的 BO
归属、放置、pin、迁移和生命周期。TTM 管理的是**作为 BO backing 的那部分 system RAM**，
不是普通进程的所有 `malloc()/mmap()` 内存。

### 6.1 TTM 的输入、状态与输出

```text
输入：
  BO size
  placement/domain要求
  VRAM/GTT/System候选位置
  pin/eviction/migration约束

TTM内部状态：
  ttm_buffer_object
  ttm_resource（当前放置资源）
  ttm_tt（system backing）
  pages[]
  dma_address[]
  caching / swap / migration / LRU状态

输出：
  CPU mmap所需的page或I/O resource
  GPUVM建PTE所需的VRAM offset或DMA address
```

TTM 回答“backing 是什么”，但**不负责决定某进程的 GPUVA 数字，也不直接维护 AMDGPU
per-process GPUVM interval tree**。那是 AMDGPU VM 层的职责。

### 6.2 `ttm_tt` 是 system-backed BO 的页面账本

核心字段：

```c
struct ttm_tt {
    struct page **pages;
    uint32_t num_pages;
    struct sg_table *sg;
    dma_addr_t *dma_address;
    ...
};
```

源码：`include/drm/ttm/ttm_tt.h:50,107-112`。

可以把它理解为：

```text
一个1 MiB GTT BO（4 KiB/page）
  num_pages = 256
  pages[0..255]       每个Host RAM page的struct page
  dma_address[0..255] 每个page对这个GPU可用的DMA地址
```

### 6.3 TTM placement 与三种常见 backing

| placement/domain    | 典型 backing                                                   | `struct page[]`角色                    |
| ------------------- | -------------------------------------------------------------- | ---------------------------------------- |
| VRAM                | GPU local memory resource/offset                               | 通常不是普通 Host RAM`struct page[]`   |
| `TTM_PL_TT` / GTT | TTM 为 BO 向 Linux MM 申请/从 TTM pool 取得的 system RAM pages | `ttm_tt.pages[]` 保存 backing pages    |
| USERPTR/外部页      | 用户或其他对象已有页面                                         | 驱动把已有 pages 接入`ttm_tt`/SG       |
| `TTM_PL_SYSTEM`   | 暂处 system/未绑定状态                                         | 可作为移动、恢复或用户页验证过程中的状态 |

### 6.4 GTT BO 的 page 从哪里来

先排除一个容易产生的图像：GTT BO 不是在驱动加载时预留好的一大段物理连续 RAM。
在这里，GTT BO 表示：

```text
一个具有GEM/TTM身份的逻辑Buffer Object
  + placement选择为TTM_PL_TT/GTT
  + backing来自普通system RAM pages
  + pages经DMA mapping/GPUVM后可被GPU访问
```

创建 1 MiB GTT BO 时，BO size 和 placement 只回答“需要 256 页且希望放在 system RAM”，
并没有自动产生 256 个具体 PFN。因此 TTM 必须为该 BO 取得真正的 backing pages。

AMDGPU GTT populate 路径：

```text
amdgpu_ttm_tt_populate()
  → ttm_pool_alloc()
  → 优先复用TTM pool中的page
  → pool需要新page时向Linux MM调用alloc_pages_node()
  → ttm->pages[]
  → dma_map_page()
  → ttm->dma_address[]
```

这里有两层管理关系：

| 层              | 管理的是什么                                                      |
| --------------- | ----------------------------------------------------------------- |
| Linux MM/buddy  | 从系统空闲 RAM 中选出具体 page frame，返回`struct page *`       |
| TTM pool/TTM BO | 代表 GPU BO 申请、缓存、持有这批 page，并记录为`ttm_tt.pages[]` |

因此“TTM 分配 GTT page”只是简写。严格说应当是：

> TTM 代表 GTT BO 从 TTM pool 取得 page；pool 需要扩容时再向 Linux 页面分配器申请 system RAM
> page。TTM 随后持有和组织这些 page 作为 BO backing。

取得 `ttm->pages[]` 的目的是把同一份 BO 数据交给两个地址空间：

```text
                              ┌─→ CPU mmap（可选）
                              │     → page_to_pfn()
Linux MM提供page              │     → vmf_insert_pfn_prot()
  → TTM持有ttm->pages[] ───────┤     → CPU PTE → CPU VA
                              │
                              └─→ GPU mapping
                                    → dma_map_page()/dma_address[]
                                    → AMDGPU VM → GPU PTE → GPUVA
```

CPU mmap 不是 GTT BO 存在的必要条件；即使 CPU 从不映射它，这批 page 仍然是保存 BO
数据和供 GPU 访问的实际 backing。如果 CPU 对 BO 执行 `mmap()`，TTM fault helper 会找到
`ttm->pages[page_offset]`，而真正的 CPU PTE 仍由 Linux VM 的 `vmf_insert_pfn_prot()` 建立。

源码锚点：

```text
drivers/gpu/drm/amd/amdgpu/amdgpu_ttm.c:1221
drivers/gpu/drm/ttm/ttm_pool.c:172   alloc_pages_node()
drivers/gpu/drm/ttm/ttm_pool.c:283   dma_map_page()
drivers/gpu/drm/ttm/ttm_bo_vm.c:249 ttm->pages[page_offset]
drivers/gpu/drm/ttm/ttm_bo_vm.c:255 page_to_pfn()
drivers/gpu/drm/ttm/ttm_bo_vm.c:266 vmf_insert_pfn_prot()
drivers/gpu/drm/amd/amdgpu/amdgpu_vm.c:1313 pages_addr = bo->tbo.ttm->dma_address
```

GTT BO 的 page 是由 Linux MM 提供、TTM 持有为 BO backing 的。但 USERPTR 的 page 已经属于
用户进程，TTM 怎样安全接入这些已有页面？这就需要 HMM。

---

## 7. HMM：查询和跟踪 CPU 页表，不是另一个 BO 管理器

HMM（Heterogeneous Memory Management）解决异构设备与 CPU page table 协作的问题：驱动拿到
用户 CPU VA 后，怎样得到当前页面；CPU `munmap/mprotect/COW/migration` 改变映射时，怎样防止
GPU 继续使用旧页？

### 7.1 HMM 的输入、状态与输出

```text
输入：
  mm_struct中的CPU VA range
  read/write/fault要求
  mmu_interval_notifier

HMM状态/结果：
  hmm_range
  hmm_pfns[]（PFN + HMM flags）
  notifier sequence

输出：
  当前有效页面的HMM PFN快照
  映射是否在查询期间失效的判断
```

HMM 不负责：

```text
不创建GEM handle
不决定TTM placement
不分配原来的用户匿名页所有权
不替AMDGPU VM选择GPUVA
```

### 7.2 HMM PFN 不是可直接使用的裸 PFN

`hmm_pfns[]` 中还编码：

```text
HMM_PFN_VALID
HMM_PFN_WRITE
HMM_PFN_ERROR
HMM_PFN_P2PDMA
HMM_PFN_DMA_MAPPED
...
```

因此 AMDGPU 使用：

```c
ttm->pages[i] =
    hmm_pfn_to_page(range->hmm_range.hmm_pfns[i]);
```

而 `hmm_pfn_to_page()` 的实现会先去掉 HMM flags：

```c
return pfn_to_page(hmm_pfn & ~HMM_PFN_FLAGS);
```

源码：

```text
include/linux/hmm.h:67
drivers/gpu/drm/amd/amdgpu/amdgpu_ttm.c:798-803
```

### 7.3 `hmm_range_fault()` 从 CPU VA 找到页面

AMDGPU 路径：

```text
amdgpu_ttm_tt_get_user_pages()
  → amdgpu_hmm_range_get_pages()
  → default_flags = HMM_PFN_REQ_FAULT
  → hmm_range_fault()
  → walk CPU page tables
  → 必要时fault-in页面
  → 填充hmm_pfns[]
```

源码：

```text
drivers/gpu/drm/amd/amdgpu/amdgpu_hmm.c:168
mm/hmm.c:659
```

### 7.4 为什么不能只查一次 PFN 永久使用

CPU 地址空间会发生：

```text
munmap
mprotect
COW替换页面
页面迁移/回收
进程退出
```

AMDGPU 用 `mmu_interval_notifier` 注册范围。CPU mapping 失效时：

```text
CPU页表准备变化
  → amdgpu_hmm_invalidate_*()
  → 标记BO/GPU mapping失效
  → 停止或阻止继续使用旧mapping
  → 解除旧DMA mapping
  → HMM重新查询page
  → 重建SG/DMA/GPU PTE
```

源码：`drivers/gpu/drm/amd/amdgpu/amdgpu_hmm.c:55-139`。

所以 HMM 的价值不只是“帮忙算 PFN”，而是让 CPU page table 仍然是用户页映射的权威来源，并让
设备 mapping 跟随它的生命周期变化。

---

## 8. 三个框架的边界：GEM、TTM、HMM 到底如何配合

### 8.1 它们不是同一层的三个候选方案

| 组件      | 管理对象                | 关键输入                        | 关键输出                                   | 不负责什么                    |
| --------- | ----------------------- | ------------------------------- | ------------------------------------------ | ----------------------------- |
| GEM       | GPU内存对象身份         | create/handle/mmap/share请求    | `drm_gem_object`引用、handle、mmap token | 不决定page住哪里              |
| TTM       | BO backing与放置        | BO、placement、迁移约束         | resource、`pages[]`、`dma_address[]`   | 不选择进程GPUVA               |
| HMM       | CPU页表镜像/跟踪        | `mm_struct`、CPU VA range     | HMM PFN/page快照、失效序列                 | 不管理GEM handle/BO placement |
| AMDGPU VM | per-process GPU mapping | GPUVA、BO、offset、DMA/VRAM地址 | GPU page-table entries                     | 不拥有原始用户页              |

### 8.2 在 USERPTR 中的合作关系

```text
GEM/amdgpu_bo：给USERPTR BO对象身份和生命周期
        │
        ▼
HMM：CPU VA → HMM PFN → struct page
        │
        ▼
TTM：把page放入ttm->pages[]，建立SG和dma_address[]
        │
        ▼
AMDGPU VM：GPUVA → dma_address[]，写GPU PTE
```

### 8.3 在普通 GTT BO 中的合作关系

```text
GEM/amdgpu_bo：创建对象、handle和mmap token
        │
        ▼
TTM：为BO从TTM pool/Linux MM取得system RAM page，持有为backing并建立dma_address[]
        │                          │
        │                          ├→ DRM mmap fault → CPU PTE → CPU VA
        │                          │
        └──────────────────────────┴→ AMDGPU VM → GPU PTE → GPUVA
```

这里不需要 HMM 从已有 CPU 页表找 page，因为页面是 TTM 代表 BO 向 Linux MM 申请并持有的。
HMM 和 TTM 并不是永远连续出现：
HMM 会用于 USERPTR、SVM 等“CPU 页表是页面权威来源”的路径，但是否再接入普通 TTM BO
取决于具体路径；本文展示的直接协作证据是 AMDGPU USERPTR BO。

### 8.4 概念都就位后，再看完整 CPU→GPU 全景图

现在才可以把前面三层压缩成一张图：

```text
进程拿到 CPU VA
  │
  ▼
mm_struct / VMA 描述这段虚拟地址区间
  │
  ▼ page fault / page-table walk
CPU PTE ──► PFN ──► struct page[]
                         │
                         ├── HMM：当CPU页表是页面权威来源时查询/跟踪
                         ├── TTM：当页面属于BO backing时组织/放置
                         ▼
                       SG table
                         │
                         ▼ dma_map_*()
                  DMA address / IOVA
                         │
                         ▼ AMDGPU VM更新
                      GPU PTE
                         │
                         ▼
                       GPUVA
```

这是**GPU 使用 Host page 的场景图**，不是普通 CPU 内存的必经路径。其中 HMM 只出现在需要
从 CPU 页表查询或跟踪页面的路径，TTM 只出现在使用 TTM BO 管理 backing 的路径。两者都不是
`struct page` 之后无条件经过的通用层。

---

## 9. 完整实例一：USERPTR BO

### 9.1 页面先属于 CPU 进程

```text
malloc/anonymous mmap
  → CPU VA
  → CPU访问触发fault
  → CPU PTE → PFN → struct page
```

libhsakmt 创建 USERPTR BO 时，把原 CPU VA 作为输入：

```c
mmap_offset = (uint64_t)mem;
ioc_flags |= KFD_IOC_ALLOC_MEM_FLAGS_USERPTR;
```

源码：`2.源码/rocr-runtime/libhsakmt/src/fmm.c:2071-2072`。

### 9.2 HMM 把 CPU 页表结果交给 TTM

```text
CPU VA
  → hmm_range_fault()
  → hmm_pfns[]
  → hmm_pfn_to_page()
  → ttm->pages[]
```

TTM bind：

```text
amdgpu_ttm_tt_pin_userptr()
  → sg_alloc_table_from_pages(ttm->pages[])
  → dma_map_sgtable(adev->dev, ...)
  → drm_prime_sg_to_dma_addr_array(..., ttm->dma_address[])
```

源码：`drivers/gpu/drm/amd/amdgpu/amdgpu_ttm.c:811-840`。

### 9.3 AMDGPU VM 用 DMA address 写 GPU PTE

```text
AMDKFD_IOC_MAP_MEMORY_TO_GPU
  → amdgpu_vm_bo_map()       记录GPUVA → BO/offset
  → amdgpu_vm_bo_update()
       pages_addr = bo->tbo.ttm->dma_address
  → amdgpu_vm_update_range()
  → amdgpu_vm_ptes_update()
  → CPU/SDMA updater写PTE
```

源码：`drivers/gpu/drm/amd/amdgpu/amdgpu_vm.c:1261-1373`。

完整链：

```text
CPU VA
  → CPU PTE/PFN
  → HMM PFN
  → struct page
  → TTM pages[]
  → SG
  → dma_addr_t/IOVA
  → GPU PTE
  → GPUVA
```

---

## 10. 完整实例二：GTT BO

### 10.1 用户态预留 VA 不会提供 BO 物理页

libhsakmt：

```text
aperture_allocate_area_aligned()
  → mmap(PROT_NONE, MAP_ANONYMOUS | MAP_NORESERVE, ...)
  → 得到预留VA
```

随后用 `KFD_IOC_ALLOC_MEM_FLAGS_GTT` 创建 BO。预留 VA 只是选择 CPU/GPU 地址数值，不是 backing。

### 10.2 Linux MM 提供 page，TTM 将其组织为 BO backing

```text
amdgpu_gem_object_create(domain=GTT)
  → amdgpu_bo_create()
  → ttm_bo_init_reserved()
  → ttm_bo_validate()
  → ttm_tt_create()
  → ttm_bo_populate()
  → ttm_pool_alloc()
       → 复用TTM pool page，或调用alloc_pages_node()
  → pages[] + dma_address[]
```

这条链中，Linux MM/buddy allocator 是 page frame 的底层提供者；TTM 是 GPU BO 的申请者和持有者。
`TTM_PL_TT/GTT` 表示 BO 使用 system RAM backing，不表示存在一块预留的物理连续“GTT RAM”。

### 10.3 同一批 page 分别进入 CPU 和 GPU 页表

CPU 侧：

```text
libhsakmt mmap(MAP_FIXED, drm_render_fd, mmap_offset)
  → CPU首次访问
  → amdgpu_gem_fault()
  → ttm_bo_vm_fault_reserved()
  → page = ttm->pages[page_offset]
  → page_to_pfn(page)
  → vmf_insert_pfn_prot()
  → CPU PTE
```

GPU 侧：

```text
AMDKFD_IOC_MAP_MEMORY_TO_GPU
  → amdgpu_vm_bo_map()
  → amdgpu_vm_bo_update()
  → ttm->dma_address[]
  → GPU PTE
```

完整链：

```text
                            ┌→ page_to_pfn() → CPU PTE → CPU VA
Linux MM提供、TTM为BO持有struct page[] ─┤
                            └→ dma_map_page() → dma_addr_t/IOVA
                                                   → GPU PTE → GPUVA
```

---

## 11. 两条路径在哪里分叉、在哪里汇合

| 阶段                   | USERPTR BO                  | GTT BO                               |
| ---------------------- | --------------------------- | ------------------------------------ |
| CPU VA                 | 原来已经存在                | libhsakmt先预留                      |
| page来源               | CPU进程原有VMA/page         | Linux MM提供，TTM pool/BO持有        |
| PFN取得                | HMM遍历CPU页表              | `page_to_pfn(ttm->pages[i])`可得到 |
| `struct page`进入TTM | `hmm_pfn_to_page()`后写入 | TTM pool取得后写入`ttm->pages[]`   |
| SG/DMA mapping         | `dma_map_sgtable()`       | TTM pool中`dma_map_page()`         |
| CPU mapping            | 原CPU mapping，不再mmap BO  | DRM mmap fault映射TTM pages          |
| GPU mapping            | `MAP_MEMORY_TO_GPU`       | `MAP_MEMORY_TO_GPU`                |
| GPU PTE来源            | `ttm->dma_address[]`      | `ttm->dma_address[]`               |

最重要的汇合点：

```text
不同页面来源
  → ttm->pages[]
  → ttm->dma_address[]
  → amdgpu_vm_bo_update()
  → per-process GPUVM
```

---

## 12. 一个数值例子贯穿 CPU VA、PFN、DMA 地址和 GPUVA

假设进程访问：

```text
CPU VA = 0x00007F00_00001ABC
```

CPU page table 查询得到：

```text
PFN         = 0x12345
page offset = 0xABC
Host page base PA = 0x12345000
完整Host PA       = 0x12345ABC
```

内核对象关系：

```text
pfn_to_page(0x12345) = page_X
ttm->pages[i]         = page_X
```

无 IOMMU 时，示意：

```text
dma_map_page(page_X) → dma_addr_t 0x12345000
GPU PTE address       = 0x12345000
GPU访问GPUVA+0xABC    → Host PA 0x12345ABC
```

有 IOMMU 时，示意：

```text
dma_map_page(page_X) → IOVA 0x80000000
Host IOMMU PTE：IOVA 0x80000000 → Host PA 0x12345000
GPU PTE address       = 0x80000000
GPU访问GPUVA+0xABC
  → IOVA 0x80000ABC
  → Host PA 0x12345ABC
```

CPU PTE 和 GPU PTE 中的地址数值可以不同，但最终落在同一个 `struct page` 对应的 Host page frame。

---

## 13. 地址与对象总对照

| 名称                     | 类型                 | 属于哪个空间/子系统          | 谁产生                          | 谁消费                  | 能否直接当PA                 |
| ------------------------ | -------------------- | ---------------------------- | ------------------------------- | ----------------------- | ---------------------------- |
| CPU VA                   | 地址值               | 进程CPU虚拟地址空间          | allocator/kernel mmap           | CPU MMU                 | 否                           |
| VMA                      | 内核对象             | Linux`mm_struct`           | mmap/brk等                      | fault/权限检查          | 否                           |
| CPU PTE                  | 映射条目             | CPU page table               | Linux MM                        | CPU page walker         | 含PFN但不是裸PA              |
| PFN                      | 页编号               | Host physical page编号       | page table/MM                   | `pfn_to_page`等       | 页面基址可左移换算           |
| `struct page *`        | Kernel VA/元数据指针 | Linux memory model           | memmap/vmemmap                  | buddy、VM、TTM、DMA API | 否                           |
| SG entry                 | 描述对象             | DMA API/驱动                 | `sg_alloc_table_from_pages`等 | DMA mapping/硬件描述符  | 否                           |
| `dma_addr_t`           | 地址值               | 某个device DMA address space | DMA API                         | 设备或GPU PTE writer    | 不保证，可能是IOVA           |
| GEM handle               | 用户可见标识         | 每个DRM file命名空间         | GEM                             | 用户态/DRM ioctl        | 否                           |
| `drm_gem_object`       | 内核对象             | DRM/GEM                      | driver/GEM                      | handle、mmap、dma-buf   | 否                           |
| `ttm_tt.pages[]`       | page指针数组         | TTM BO backing               | TTM或HMM→driver                | CPU fault、SG构建       | 否                           |
| `ttm_tt.dma_address[]` | DMA地址数组          | TTM/device视角               | DMA API                         | AMDGPU VM updater       | 不保证是Host PA              |
| GPUVA                    | 地址值               | 某进程GPUVM                  | UMD/KFD mapping                 | GPU MMU                 | 否                           |
| GPU PTE                  | 映射条目             | GPU page table               | AMDGPU VM                       | GPU page walker         | address部分可为VRAM或DMA地址 |

---

## 14. 常见误区：看到一个词先判断它属于哪一类

| 误区                              | 正确判断                                                    |
| --------------------------------- | ----------------------------------------------------------- |
| 把 PFN 当作完整 PA                | PFN 是页编号；完整PA还要加page offset                       |
| `struct page *` 是物理地址      | 它是指向页面元数据的Kernel VA                               |
| 有 CPU VA 就已经有物理页          | VMA/VA可先存在，page可能在fault时才出现                     |
| `mmap_offset` 是PA              | DRM/KFD中常是token，USERPTR场景还可能复用为输入CPU VA       |
| GPU 能直接使用CPU VA              | 常规路径必须建立GPUVA/GPU PTE；SVM共享数值也不是共享页表    |
| PFN算出PA后可绕过DMA API          | 设备应使用DMA API返回的`dma_addr_t`                       |
| 认为USERPTR页面由HMM分配          | 页面属于CPU VMA；HMM查询、fault-in并跟踪映射                |
| TTM 与HMM是二选一                 | TTM管BO backing；HMM管CPU页表协作，USERPTR中会配合          |
| 把 backing 管理与页表更新混成一层 | TTM提供resource/page/DMA地址；进程PTE由AMDGPU VM层更新      |
| GEM就是显存分配器                 | GEM主要管对象身份、handle、mmap与共享                       |
| GTT、GART、GPUVM是一回事          | GTT是backing域，GART是全局aperture映射，GPUVM是每进程VA映射 |

---

## 15. 与整套串联文档如何连接

本文解决“页面、地址和内存对象是什么”，其他章节分别沿不同方向继续：

```text
[3. Linux进程基础]
  task_struct / mm_struct / 线程共享地址空间
              │
              ▼
[2A. 本文]
  CPU VA → PFN → struct page → DMA address
  GEM/TTM/HMM对象边界
              │
       ┌──────┼───────────────┐
       ▼      ▼               ▼
[2. 常规内存] [7. MMU/GPUVM] [10. PCIe/DMA]
 API/BO/copy   页表建写切换    BAR/IOMMU/TLP
       │          │               │
       └──────────┴──────┬────────┘
                         ▼
                  [9. AMD SVM]
                fault/迁移/一致性
```

建议阅读顺序不是单一线性顺序，而是先建立共同地基，再按问题分叉：

1. 想理解“进程与地址空间是谁的”：先读 [第 3 章](<./1.笔记/3. Linux 进程基础：task_struct、mm 与线程.md>)。
2. 想理解“页面与GPU内存对象是什么”：读本文。
3. 想理解“API怎样分配和搬运”：读 [第 2 章](<./1.笔记/2. 常规内存机制：GPU 内存分配与 CPU↔GPU 访问.md>)。
4. 想理解“CPU/GPU页表怎样建立和切换”：读 [第 7 章](<./1.笔记/7. MMU 与页表：从 CPU 对照看 GPUVM 的构建、写入与切换.md>)。
5. 想理解“任意指针、fault和迁移”：读 [第 9 章](<./1.笔记/9. AMD SVM：共享内存、统一地址空间与页面迁移.md>)。
6. 想理解“地址最终怎样变成PCIe事务”：读 [第 10 章](<./1.笔记/10. AMD PCIe 地址映射与 DMA：GPU Endpoint.md>)。

整套文档的宏观模型最终是：

```text
进程身份与mm
  → 虚拟地址与物理页
  → GPU内存对象与backing
  → CPU/GPU/IOMMU三套地址翻译
  → 队列提交与硬件执行
  → fault/interrupt/completion
  → 多进程隔离和资源回收
```

---

## 16. 源码证据索引

| 主题                          | 源码锚点                                                           |
| ----------------------------- | ------------------------------------------------------------------ |
| PFN↔PA                       | `include/linux/pfn.h:12-13`                                      |
| PFN↔`struct page`          | `include/asm-generic/memory_model.h:18-87`                       |
| `struct page`定义           | `include/linux/mm_types.h:80`                                    |
| CPU page fault入口            | `mm/memory.c:6651 handle_mm_fault()`                             |
| HMM PFN flags与转换           | `include/linux/hmm.h:16-78`                                      |
| HMM页表遍历                   | `mm/hmm.c:659 hmm_range_fault()`                                 |
| AMD HMM注册/失效              | `drivers/gpu/drm/amd/amdgpu/amdgpu_hmm.c:55-168`                 |
| TTM system page账本           | `include/drm/ttm/ttm_tt.h:50-112`                                |
| GTT page底层分配/TTM pool持有 | `drivers/gpu/drm/ttm/ttm_pool.c:172,283,873`                     |
| AMD USERPTR PFN→page         | `drivers/gpu/drm/amd/amdgpu/amdgpu_ttm.c:798-803`                |
| AMD USERPTR SG/DMA map        | `drivers/gpu/drm/amd/amdgpu/amdgpu_ttm.c:811-840`                |
| AMD GTT populate              | `drivers/gpu/drm/amd/amdgpu/amdgpu_ttm.c:1221-1249`              |
| GEM handle                    | `drivers/gpu/drm/drm_gem.c:552 drm_gem_handle_create()`          |
| GEM mmap                      | `drivers/gpu/drm/drm_gem.c:1377 drm_gem_mmap()`                  |
| CPU mmap TTM page             | `drivers/gpu/drm/ttm/ttm_bo_vm.c:183 ttm_bo_vm_fault_reserved()` |
| KFD USERPTR/GTT分流           | `drivers/gpu/drm/amd/amdgpu/amdgpu_amdkfd_gpuvm.c:1709-1871`     |
| GPUVM读取DMA地址              | `drivers/gpu/drm/amd/amdgpu/amdgpu_vm.c:1261-1373`               |
| CPU updater写GPU PTE          | `drivers/gpu/drm/amd/amdgpu/amdgpu_vm_cpu.c:73-110`              |
| SDMA updater写GPU PTE         | `drivers/gpu/drm/amd/amdgpu/amdgpu_vm_sdma.c:219-294`            |
| libhsakmt USERPTR/GTT         | `2.源码/rocr-runtime/libhsakmt/src/fmm.c:2049-2088`              |
